{
  "name": "playwright-qa-portfolio",
  "version": "1.0.0",
  "description": "Portfolio de automacao de testes (Web UI + API) com Playwright e TypeScript, seguindo o padrao Page Object Model.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "mock-app": "node mock-app/server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
